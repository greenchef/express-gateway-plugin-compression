{
  "name": "express-gateway-plugin-compression",
  "version": "0.0.0",
  "description": "A compression plugin for Express Gateway.",
  "main": "manifest.js",
  "scripts": {
    "test": "jest --env node"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "compression": "1.7.4"
  }
}
